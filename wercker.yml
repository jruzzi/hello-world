box:
  id: openjdk
  ports:
    - "8080"
    
dev:

  steps:
    
    - script:
      name: run mvn
      code: |
        mvn exec:java

build:

  steps:
  
   - script:
     name: run mvn
     code: | 
      mvn clean test
    
